# Deploy thread

**Deploy thread** — один Slack-тред для всех deploy-событий по одной Jira-задаче.

Он начинается сообщением `Ready for Deploy`, которое бот отправляет при переходе задачи в статус `Ready for Deploy`. Дальше связанные уведомления о pipeline для этой же задачи публикуются ответами в этот же тред, а не отдельными сообщениями в канал.

## Зачем это нужно

- В канале остаётся одно корневое сообщение на задачу.
- История деплоя собирается в одном месте: созданные MR, stage pipeline, prod pipeline.
- Проще понять текущее состояние задачи без поиска по нескольким сообщениям.
- Меньше шума в общем Slack-канале.

## Какие события входят в тред

1. `Ready for Deploy` — корневое сообщение треда.
2. Результат создания или нахождения MR в привязанных GitLab-репозиториях.
3. Уведомление о завершении watched pipeline в `master` или `main`.
4. Уведомление о manual pipeline, когда stage завершён и prod ждёт ручного запуска.
5. Итоговое сообщение о завершении деплоя задачи, когда все MR этой задачи получили successful prod pipeline.

## Как связывается задача и pipeline

Бот берёт Jira-key из заголовка коммита pipeline, например `CPAYMENT-1000`.

Если по этому ключу уже есть сохранённый Slack `thread_ts` от сообщения `Ready for Deploy`, pipeline-уведомление отправляется в этот тред.

Если тред не найден, бот отправляет pipeline-уведомление отдельным сообщением, как раньше.

## Как определяется завершение деплоя задачи

При `Ready for Deploy` бот запоминает все MR, которые были созданы или найдены для Jira-задачи. Если в одном GitLab-проекте найдено несколько веток с ключом задачи, каждая ветка обрабатывается как отдельный MR.

Когда GitLab присылает successful pipeline по `master` или `main`, бот отмечает следующий ожидающий MR этой задачи в том же проекте и целевой ветке как доставленный в prod. После успешных prod pipeline для всех MR задачи бот пишет в deploy thread итоговое сообщение с упоминанием ответственных и ссылкой на Jira.

## Ограничения

- Связь `Jira-key → Slack thread_ts` хранится в памяти процесса.
- Состояние ожидаемых MR и завершённых prod pipeline хранится в памяти процесса.
- После рестарта сервиса уже созданные треды не восстанавливаются автоматически.
- Текущий срок хранения связи в памяти — 7 дней.
- Pipeline попадёт в тред только если ключ задачи есть в первом заголовке commit message.

## Как ссылаться в задачах

Используйте формулировку:

> Deploy thread: все Slack-уведомления по деплою одной Jira-задачи собираются в одном треде, начиная с сообщения `Ready for Deploy`.

Ссылка на документ: [`docs/deploy-thread.md`](./deploy-thread.md)
